services:
  opensprint:
    image: ghcr.io/YOUR_GITHUB_USERNAME/opensprint:latest
    container_name: opensprint
    restart: unless-stopped
    env_file:
      - /opt/secrets/opensprint.env
    networks:
      - frontend
      - backend

networks:
  frontend:
    external: true
  backend:
    external: true
